services:
  - type: web
    name: flask-php-app
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y php-cli
      pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
